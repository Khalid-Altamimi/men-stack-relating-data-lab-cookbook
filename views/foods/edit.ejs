<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add new Food</title>
  </head>
  <body>
    <%- include('./partials/_navbar.ejs') %>
    <h1>Edit <%= food.name %> </h1>

    <form action="/users/<%= userId %>/foods/<%= food._id %>?_method=PUT" method="POST">
        <label for="name">Food Name:</label>
        <input type="text" id="name" name="name" value="<%= food.name %>" required>

        <label for="category">Category:</label>
        <input type="text" id="category" name="category" value="<%= food.category %>">

        <label for="quantity">Quantity:</label>
        <input type="number" id="quantity" name="quantity" min="1" value="<%= food.quantity %>">

        <button type="submit">Update Item</button>

        </form>
  </body>
</html>
